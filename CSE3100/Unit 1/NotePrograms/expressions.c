#include <stdio.h>

void main() {
	printf("ternary: exp1 ? True : False\n");
	printf("for printf %%d = integers %%c = characters %%p = pointers\n");
	printf("void *malloc(sive_t size);, use the sizeof() function,\t void * calloc(size_t num, size_t size);\n");
